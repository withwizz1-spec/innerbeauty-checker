# InnerBeauty-Checker

## 프로젝트 개요
한국 올리브영·약국 등에서 판매되는 건강기능식품을 대상으로, 제품명 검색 또는 성분표 사진 촬영을 통해 각 원료 성분의 안전성을 분석해주는 웹 서비스.

**한 줄 설명:** 제품명 검색 또는 성분표 사진 촬영으로 원료의 안전성을 분석해주는 한국 건강기능식품 특화 앱

**경쟁 서비스 분석:** 푸드스캐너(2026.03 출시)가 유사한 방향이지만, 다음 한계가 확인됨
- 제품 DB 부족 (올리브영 취급 건강식품 미등록)
- 동명 제품 구분 불가 (제품 이미지 없음, 제조사명만 표시)

## ⚠️ "성분 검색"의 목적 (오해 금지 — 반복 정정된 사항)
이 앱에서 **성분을 확인해준다는 건 "사람들이 잘 모르는 낯선 원료"를 알려준다는 뜻**이다.
비타민C·루테인·오메가3처럼 이미 유명하고 식약처 판정도 끝난 기능성 원료를 비교·정렬해주는 게 아니다.

- 사용자가 겪는 진짜 문제: 성분표에서 **처음 보는 이름**을 만났을 때 이게 뭐고 안전한지 알 수 없음
- 따라서 기능 제안·설계 시 "유명 성분으로 제품 비교" 방향을 기본값으로 삼지 말 것
- 대신 **낯선 원료를 눈에 띄게 만들고 설명해주는 방향**을 우선할 것 (미확인 등급 노출, 원료 설명 등)
- 위 "성분 분류 기준"의 `미확인 ≠ 미검증` 원칙과 같은 맥락

## 차별화 포인트
- **제품 이미지 노출** — 동명 제품 혼동 방지, 사용자가 구매한 제품을 직관적으로 확인
- **건강기능식품 특화 DB** — 식약처 공공 API 기반, 올리브영·약국 유통 제품 우선 확보
- **성분 간 상호작용 분석** — A + B 성분을 함께 섭취할 때의 효과/주의사항 제공
- **개인 맞춤 필터** — 임산부·노인·특정 질환자 등 상황별 주의 성분 강조
- **건강기능식품 / 건강보조식품 구분 표시** — 식약처 정식 인증 여부를 명확히 안내

## 용어 구분 (중요)
- **건강기능식품**: 식약처로부터 과학적 근거를 통해 기능성·안전성을 인정받은 제품. 식약처 "건강기능식품 정보" 공공 API에 등록되어 있음.
- **건강보조식품**: 식약처의 공식 인증을 받지 않은 일반 식품. 건강에 좋다고 알려져 있지만 기능성이 검증되지 않음. 위 API에 등록되어 있지 않음.
- **판별 로직**: 별도 분류 알고리즘 없이, 식약처 API 등록 여부 자체를 기준으로 자동 분류 (등록 O → 건강기능식품 뱃지, 등록 X → 건강보조식품 뱃지)

## 성분 분류 기준 (신뢰도 참고 — 중요)
제품 상세페이지의 원재료는 **기능성원료 / 첨가물 / 미확인** 3가지로 자동 분류되고, 각 성분은 **경고 / 안전 / 미확인** 등급을 받음. 이 분류·등급 로직은 각각 근거 신뢰도가 다르므로 구분해서 이해할 것:

| 데이터 | 출처 | 신뢰도 |
|---|---|---|
| 제품별 공식 기능성 문구 (PRIMARY_FNCLTY) | **식품안전나라 C003 API 응답 필드** — 식약처가 그 제품에 대해 직접 인정한 기능성 문구 | 공식 근거 있음 (제품별로 확인 가능) |
| 기능성원료 / 첨가물 하드코딩 사전 (폴백용) | **자체 큐레이션** (자주 등장하는 성분 위주로 직접 정리한 1차 버전, 공식 원료 DB 대조 아직 안 함) | 참고용. 오분류 가능성 있음 |
| 알레르기 유발물질 목록 (19종) | 식약처(식품안전나라) 공식 표시 대상 목록 | 공식 근거 있음 |
| 해외 논란 첨가물 (6종) | WHO/IARC, EU 집행위, 영국 FSA, 미국 CSPI 등 개별 출처 명시 | 공식 근거 있음 (단, "금지/위험"이 아니라 "논란이 있었다"는 참고 정보) |

- **판정 순서**: 이 제품의 공식 기능성 문구(PRIMARY_FNCLTY)에 성분명이 있으면 `기능성원료` (최우선, 제품별 공식 근거) → 첨가물 사전 → 기능성원료 사전(자체 추정, 폴백) → 이름 패턴 추측(`~색소`, `~향` 등) → 미확인
- **미확인 ≠ 미검증**: 공식 문구에도 없고 사전에도 이름이 없어 자동 분류를 못 한 것일 뿐. 해당 원료가 포함된 제품 자체는 이미 식약처 승인을 받았으므로 원료도 그 심사 과정에서 검토된 상태
- **등급 판정**: 알레르기 유발물질 또는 논란 첨가물에 해당하면 `경고`, 분류가 되면(기능성원료·첨가물 상관없이) `안전`, 분류 자체가 안 되면 `미확인`. 첨가물이라서 등급이 낮아지지 않음 (첨가물도 자체 안전성 심사를 거친 성분)
- 정확한 사전 목록·매칭 로직은 `src/data/ingredientCategory.js`, `src/data/allergens.js`, `src/data/controversialAdditives.js`, `src/data/ingredientGrade.js` 참고

### 사전 보강 후보 (미착수, 난이도순)
- 🟢 ~~제품별 공식 기능성 문구 우선 확인~~ — 완료
- 🟡 건강기능식품 개별인정형 정보 API (`data.go.kr/data/15074311`, foodsafetykorea `svc_no=I-0050`) — 별도 활용신청 필요, "개별인정형"만 포함(표준 원료는 제외)
- 🟡 원료별 정보 게시판 엑셀 다운로드 (`foodsafetykorea.go.kr` 게시판, 메뉴 2660) — 버튼이 JS(`fn_excelDown()`)라 브라우저 자동화 필요
- 🔴 건강기능식품 공전(고시형 원료 기준·규격 문서) — 정확한 문서 링크 미확인, 별도 조사 필요

## 기술 스택
- 프론트엔드: React (Vite)
- 백엔드: Python (FastAPI) — Phase 2에서 신규 구축 예정
- 데이터 소스: 식품안전나라(foodsafetykorea.go.kr) 오픈API — 건강기능식품 품목제조신고(원재료) 서비스, `C003`
- OCR / AI 분석: Claude API (이미지 입력) — 후순위(Phase 4)로 조정. 종량제 과금 구조라 별도 검토 필요, 백엔드 완성 후 진행
- 배포: Vercel 또는 Netlify (백엔드는 Railway/Render 등 별도 호스팅 필요)
---

## 나에 대해
- Python 기본 지식 있음 / 웹·앱 실개발 경험 없음
- 현재는 개인 학습 목적이며, 결과물에 따라 실제 배포도 고려 중
- MZ세대 타겟, 비주얼이 트렌디하고 창의적인 UI 지향

## Claude의 역할
- 앱/웹 아이디어를 말하면 목업 화면 → 주요 기능 → 요구사항 순서로 제시
- 아이디어가 모호하면 목적·타겟·사용 환경을 질문하거나 선택지를 줘서 구체화
- 코드는 핵심 로직에만 짧은 주석 달기. 장황한 설명 불필요
- 처음 보는 개념이나 도구가 나오면 한 줄 정도로 간단히 설명
- 로드맵과 일정표는 요청할 때만 시각화해서 제공

## 코드 작성 원칙
- 나중에 배포를 고려해 처음부터 폴더/컴포넌트 구조는 깔끔하게 잡기
- 지금은 학습 단계이므로 복잡한 최적화보다 이해하기 쉬운 코드 우선
- 기능 구현 우선, UI/UX 폴리시는 후순위 (Phase 3에서 집중)
- 한 번에 기능 하나씩 작업 (배치 변경 지양)

---

## 개발 로드맵

### Phase 1 — 기능 우선 (웹앱) ✅ 완료
**목표:** 핵심 기능이 동작하는 React 웹앱 완성

- [x] 프로젝트 셋업 (Vite + React)
- [x] 식약처 건강기능식품 공공 API 연동 (식품안전나라 C003)
- [x] 제품명 텍스트 검색 → 결과 리스트 출력
- [x] 제품 상세 페이지 (성분 목록 + 등급 표시)
- [x] 건강기능식품 / 건강보조식품 뱃지 표시 (API 등록 여부 기준 자동 분류)
- [x] 성분 등급 판정 로직 구현 (안전 / 경고 / 미확인 3단계 — "주의" 단계는 첨가물을 부당하게 낮게 보이게 해 제외)
- [x] 성분 상세 페이지 (역할·위험도·참고 기관)
- [x] (추가) 원재료명 개별 성분 파싱 + 기능성원료/첨가물 자동 분류
- [x] (추가) 성분 구성 비율 도넛 차트 (가짓수 기준)
- [x] (추가) 알레르기 유발물질(19종) · 해외 논란 첨가물(6종) 하이라이트
- [x] (추가) 기능성원료·첨가물·미확인 개념 설명 패널

### Phase 2 — 백엔드 구축 (Python/FastAPI) ✅ 핵심 작업 완료 (실 데이터 검증만 대기)
**목표:** 식약처 API 호출을 프론트엔드에서 백엔드로 이전 — 배포 가능한 구조로 전환

**배경:** 지금은 `vite.config.js` 프록시로 CORS를 우회하는데, 이건 `npm run dev`(로컬 개발)에서만 동작함. 배포하면 프록시가 사라져서 API 호출이 막히고, API 키도 브라우저 번들에 그대로 노출되고 있음 — 배포 전 반드시 해결해야 하는 구조적 문제.

- [x] FastAPI 프로젝트 셋업 (`backend/`, Python 3.12 가상환경 — 시스템 Python 3.9.6은 EOL이라 Homebrew로 신규 설치)
- [x] 식약처 API 프록시 엔드포인트 구현 (`/api/search`, `/api/detail`)
- [x] API 키를 서버 환경변수로 이동 (`backend/.env`, 프론트엔드 `.env.local`에서 제거)
- [x] 프론트엔드가 식약처 API 대신 우리 백엔드를 호출하도록 변경 (`VITE_BACKEND_URL`, Vite 프록시 설정 제거)
- [x] 프론트→백엔드→외부API 전체 배선 Playwright로 end-to-end 검증 (에러 응답 전달까지 확인)
- [ ] (선택, Phase 3으로 이월) 검색 결과 캐싱

**참고:**
- 식품안전나라 OPEN-API가 2026-07-07(수)부터 **제한적 운영 중** (공공데이터 서비스 서버 불안정, 종료일 별도 공지 예정) — 이용 가능 시간은 **저녁 7시~다음날 오전 8시**뿐이라 주간에는 항상 ERROR-500이 남. 우리 코드/키 문제 아님(공식 공지 확인됨). 실 데이터 검증은 저 시간대에 재시도하거나, 매 작업 시작 시 실제 호출로 현재 상태만 확인하고 넘어갈 것 (공지 종료일을 매번 찾아볼 필요 없음).
  - **[2026-07-13 정상화] 위 장애 해소됨.** 유지보수팀에 직접 문의 → 답변 확인: 2026-07-10~12은 서버 장애로 종일(저녁 포함) 불가였고, 2026-07-13 19시부터 제한운영시간(09~19시) 외 시간대에 Open-API 정상 이용 가능하도록 조치 완료. 같은 날 19시 이후 실호출로 C003·C002 모두 정상 응답 확인(C003 4,708건·C002 1,757건, 원재료명 정상 반환). 정리: **현재는 매일 09~19시만 제한, 그 외(19시~익일 09시) 정상.** 원인이 우리 코드·인증키가 아님도 공식 확정.
- `/api/detail` 엔드포인트는 구현은 됐지만, 프론트엔드가 아직 실제로 호출하지 않음 (상세 페이지가 검색 결과 데이터를 재사용 중이라 당장은 불필요) — 나중에 필요해지면 바로 쓸 수 있게 준비만 된 상태.
- **[2026-07-22 발견/수정] "09~19시 제한"과는 별개의 원인으로도 검색이 500으로 죽는 버그가 있었음** — 식품안전나라 응답이 (제한 시간 밖에서도) 14~18초씩 걸리는 경우가 실제로 있는데, `httpx.AsyncClient()`가 기본 타임아웃(5초)만 쓰고 있어서 서버가 정상 응답 중인데도 우리 쪽이 먼저 타임아웃으로 끊어버리고, 그마저도 예외 처리가 없어 원인불명의 500으로 죽었음. `search_service.py`/`data_go_kr.py` 타임아웃을 20초로 늘리고 타임아웃·응답파싱 실패를 502/504로 명확히 구분해 처리하도록 수정, 회귀 테스트 2건 추가. **다만 응답 자체가 느린 건(14~18초) 여전히 남아있는 외부 API 특성** — 검색이 느리게 체감될 수 있음
- **[2026-08-11 발견 → 08-12 수정] 두 단어 이상 검색하면 무조건 0건이던 문제** — 식약처는 `PRDLST_NM`을 문자열 전체로 통째 대조하는데, 사용자가 치는 이름은 쇼핑몰 표기대로 "브랜드 + 제품명"이 붙은 형태(`이너랩 홀드잇`·`비비랩 콜라겐`·`종근당건강 락토핏` 전부 0건, 한 단어로 줄이면 각각 2·1·57건). `search_service.py`를 `_search_single()`(단일 조회, 기존 캐시 로직 재사용) + `_tokenize()`(공백 분해, 2글자 이상·중복 제거·최대 3개) + `_merge_token_results()`(중복 제거 후 여러 토큰에 걸린 제품을 상단으로) 구조로 재작성하고, **0건일 때만** `asyncio.gather(..., return_exceptions=True)`로 병렬 재조회하도록 수정(외부 API가 느려서 순차 호출은 비현실적, 단어 1개면 재시도 안 함). 한글 검색어 `urllib.parse.quote()` 인코딩도 함께 추가. pytest 2건 추가·통과 — **실데이터 검증은 미완(19시 이후 확인 필요)**
  - 참고: 이전에 "브랜드명은 API에 없다"고 진단했던 건 부정확했음. 별도 브랜드 필드가 없는 건 맞지만 제품명 안에 브랜드가 들어있는 경우가 대부분(`이너랩` 6건, `락토핏` 57건) — `홀드잇`이 예외적으로 브랜드가 안 붙은 케이스였을 뿐
- **[2026-08-13~16] 분해 재시도의 '정렬'을 세 번 고쳐서 완성** — 결과는 나오는데 정작 찾던 제품이 뒤로 밀리는 문제. 실데이터로 검증하며 두 번 실패한 뒤 해결
  1. **토큰 개수**: 두 토큰을 다 가진 제품이 없으면 전부 동점 → 입력 순서(앞 단어)가 이김. `이너랩 홀드잇`에서 홀드잇 제품이 7위
  2. **희소성(1/totalCount, IDF 발상)**: 이너랩 케이스는 해결됐으나 `종근당건강 락토핏`에서 **역효과** — 락토핏이 인기 제품군이라 57건, 종근당건강은 19건이라 락토핏이 '흔한 단어'로 취급돼 11~20위로 밀림
  3. **접두 일치 가산점**: 양쪽 다 자기 토큰으로 시작해 **가산점이 상쇄**되어 무의미
  4. **최종 — 위치를 주 신호로**: 뒤쪽 토큰일수록 높은 점수(한국 제품명은 `브랜드 + 제품명` 순이고 사용자도 그 순서로 검색). 접두 일치·희소성은 보조로 더함. 희소성을 남긴 이유는 `종근당건강 락토핏 골드`처럼 토큰이 3개 이상일 때 맨 뒤 흔한 수식어가 1위가 되는 것을 눌러주기 위함. **실데이터로 두 케이스 모두 상위 노출 확인**, 테스트 3건으로 고정
- **[2026-08-13 발견/수정] half-open 연결 하나로 백엔드 전체가 무응답이 된 사건** — 19시 검증 중 모든 API가 멎음(curl 5분 49초 후 `HTTP 000`). 업스트림(0.9초)·Postgres(2.2초)는 정상인데 `pg_stat_activity`에 우리 세션이 **0개**이면서 `lsof`상 DB 포트로 **ESTABLISHED**를 물고 있었음 → 중간 장비가 조용히 끊었는데 종료 신호가 안 온 half-open. `psycopg2`가 동기(blocking)라 이 하나가 async 이벤트 루프를 통째로 붙잡아 서버 전체가 멈춤. `db.py`에 `connect_timeout=10`·TCP keepalive(30/10/3)·`statement_timeout=15s` 추가 → 같은 상황에서 2.6초 만에 502+원인 메시지로 개선
  - ⚠️ **근본 원인은 미해결** — 동기 DB 호출이 이벤트 루프를 잡는 구조는 그대로라 15초 동안은 여전히 다른 요청이 대기함. 같은 이유로 `asyncio.gather` 병렬 조회도 실제로는 직렬 실행돼 **첫 조회에서 토큰 하나가 타임아웃으로 누락**되는 현상이 남아있음(캐시가 채워지면 정상). 제대로 고치려면 `run_in_executor`로 빼거나 asyncpg로 교체해야 하며 `db.py`·`auth.py`·`ingredients.py` 등 전부 손봐야 함

### Phase 3 — 백엔드 심화 (포트폴리오 목적) ✅ 완료
**목표:** Phase 2의 "프록시 서버" 수준을 넘어, 실제 백엔드 역량(DB·인증·자동화)을 보여줄 수 있는 기능 추가

**배경:** Phase 2는 프론트→백엔드→식약처 API로 그대로 전달하는 "프록시" 역할이라 백엔드 경험으로는 다소 얕음. 프론트/백엔드 모두 다루는 포트폴리오를 준비 중이라, 아래 항목으로 백엔드 쪽 깊이를 더함.

- [x] DB 연동 (SQLite, `backend/db.py`) — 검색 결과 캐싱(TTL 1시간), 캐시 read/write 로직 및 외부 API 오류는 캐싱하지 않는 것까지 검증 완료
  - 부가 발견/수정: 식품안전나라가 HTTP 200 + 본문 내 `ERROR-500`으로 응답하는 케이스를 놓치고 있던 버그 발견 → `parse_service_result()`로 본문 코드까지 확인하도록 수정
  - 만료된 캐시 행 자동 삭제 (`_delete_expired()`, 캐시 저장 시마다 실행) — TTL 지난 행이 삭제 안 되고 계속 쌓여 `cache.db`가 무한정 커지는 문제 방지
- [x] 성분 분류 사전 DB화 (`backend/ingredients.py`) — 기존 프론트 하드코딩 사전(기능성원료 52개, 첨가물 24개)을 SQLite `ingredients` 테이블로 이전
  - `GET /api/ingredients/categories`: 사전 전체를 프론트가 앱 시작 시 한 번에 받아와 캐싱(`setCategoryDict()`)
  - `POST /api/ingredients/report`: 오분류 신고 접수(자동 반영은 안 하고 `ingredient_reports` 테이블에 적재, 추후 검토용) — `IngredientDetail.jsx`에 신고 UI 추가
  - PRIMARY_FNCLTY 기반 판정(제품별 공식 근거)은 그대로 프론트에 유지 — 로컬 동기 연산이라 백엔드 왕복 불필요
- [x] 사용자 계정 + 개인화 설정 (`backend/auth.py`) — 이메일+비밀번호 회원가입/로그인(bcrypt 해싱, JWT 토큰), 임산부·노인 모드 저장 및 실제 성분 하이라이트 연동까지 완료
  - `POST /api/auth/signup`, `/login`: JWT 토큰 발급 / `GET /api/auth/me`, `PUT /api/auth/settings`: `Authorization: Bearer` 헤더로 인증 (FastAPI `Depends`)
  - `backend/mode_warnings.py`: 모드별(임산부/노인) 주의 성분 사전(1차 버전, 출처 명시) — `GET /api/mode-warnings?mode=`으로 조회
  - 프론트(`AuthPanel.jsx`): 로그인/회원가입 폼 + 모드 선택 UI, 토큰은 `localStorage`에 저장해 새로고침에도 로그인 유지
  - 선택된 모드의 주의 성분은 원재료 칩에 🔔 보라색 하이라이트로 표시, 성분 상세 페이지에도 사유 노출 — Playwright로 회원가입→모드 선택→하이라이트→새로고침 유지→로그아웃까지 전체 검증
  - **[2026-07-22 추가] 알레르기 다중 선택 기능** — `user_settings`에 `allergies`(JSON) 컬럼 추가, `PUT /api/auth/settings`가 `health_mode`/`allergies` 중 보낸 필드만 갱신하도록 일반화. 선택한 알레르기와 겹치는 원재료는 제품 상세 칩에 🚫 빨간 하이라이트로 표시(모드 주의사항 🔔 하이라이트와 별개 표시) — pytest 추가, Playwright로 저장→새로고침 유지→제품 상세 하이라이트까지 검증
  - **[2026-07-22 발견/수정] CORS 멀티포트 버그** — 백엔드 CORS가 `localhost:5173` 고정이었는데, 이전 세션들이 vite 개발 서버를 안 끄고 방치(5173/5174/5175 동시 실행)한 상태에서 실사용 포트가 밀리면 회원가입 등 모든 API가 CORS로 조용히 막히는 버그였음 → `allow_origin_regex=r"http://localhost:\d+"`로 완화. **세션 시작 시 방치된 중복 dev 서버가 있는지 먼저 확인할 것**
- [x] **찜하기 + 내 영양제 리스트 (2026-08-16, `backend/favorites.py`)** — 복용 중인 제품을 담아두고, 그중 함께 먹는 것만 골라 궁합을 확인하는 기능
  - `favorites` 테이블: `user_id` + `product_key` + `product`(JSONB) + `created_at`, `(user_id, product_key)` 유니크
  - **제품 정보를 통째로 저장**하는 게 핵심 설계 — 식약처 API는 신고번호로 재조회할 수단이 없고(제품명 검색만 가능) 09~19시엔 막혀서, 키만 저장하면 **찜 목록조차 못 여는** 상황이 생김. 원재료명이 있어야 궁합 분석도 가능
  - `GET/POST /api/favorites`, `DELETE /api/favorites/{key}` — pytest 9건(로그인 필수 401, 중복 찜 시 갱신, 한글·`|` 섞인 키 URL 인코딩, **다른 사용자의 찜은 보이지도 지워지지도 않음** 등)
  - 프론트: 제품 카드·상세에 하트 버튼(색만이 아니라 빈 하트/채워진 하트로 구분), 비로그인 시 안내 모달 → 로그인 유도, nav 드롭다운에 `내 영양제 리스트`(찜 개수 표시)
  - **궁합 분석**은 찜한 것 전부가 아니라 **체크한 제품끼리만** — 영양제를 항상 다 같이 먹는 게 아니므로. 기존 상호작용 사전 8건과 `findInteractions`를 그대로 재사용하고 범위만 '한 제품 안' → '선택한 제품들'로 넓힘. 상호작용이 없을 땐 "없다고 해서 반드시 안전한 건 아니에요" 문구를 함께 표시
  - 발견/수정 2건: ① CORS `allow_methods`에 **`DELETE`가 빠져 있어** 찜 해제가 브라우저에서만 조용히 막힐 뻔함 ② `fetchMe` 실패를 종류 구분 없이 전부 로그아웃 처리해서 **백엔드가 잠깐 죽으면 새로고침 시 로그인이 풀리던** 버그 → 401일 때만 로그아웃하도록 수정
- [x] 백그라운드 동기화 잡 (`backend/scheduler.py`) — 인기 검색어 5개(비타민C·오메가3·유산균·홍삼·루테인)를 1시간마다 자동으로 미리 검색해 캐시를 채워둠 (APScheduler `AsyncIOScheduler`, 서버 시작 시 즉시 1회 + 이후 매시간)
  - 검색 로직을 `backend/search_service.py`로 분리 — `/api/search` 라우트와 스케줄러가 동일한 함수를 재사용
  - `GET /api/sync-status`: 최근 동기화 실행 기록(시각·성공/실패 개수) 조회 — `sync_log` 테이블에 매 실행마다 적재
  - 실제 서버로 동작 검증: 시작 직후 5개 키워드 모두 성공 기록, 캐시가 미리 채워져 있어 인기 키워드 검색 시 외부 API 호출 없이 즉시 응답되는 것까지 확인
  - **[2026-08-11 발견 → 08-20 해결] 인기 검색어 5개가 전부 성분명이라 앱 성격과 안 맞던 문제** — 이 앱은 "성분 사전"이 아니라 "제품 확인" 서비스라, 미리 캐싱해둘 키워드도 사용자가 실제로 칠 법한 제품/브랜드명이어야 맞음 → `락토핏·이너랩·비비랩·닥터린·홀드잇`(제품명) + `유산균`(성분명 1개 유지)으로 교체. 19시 이후 실호출로 6개 전부 결과 확인(57·6·1·18·2·2,921건), 실서버 스케줄러 실행에서 **성공 6 / 실패 0** 기록까지 검증
    - ⚠️ **키워드는 반드시 한 단어로 둘 것** — `종근당건강 락토핏`처럼 붙이면 식약처가 0건을 반환해 `search_service`의 분해 재시도를 타면서 외부 API 호출 수가 몇 배로 늘어남 (코드 주석으로도 남겨둠)
- [x] API 테스트(pytest) + 자동 문서화(FastAPI Swagger) — 엔드포인트 17개 테스트 케이스 작성, Swagger 태그/설명 정리
  - `backend/conftest.py`: 테스트 전용 SQLite 파일로 격리(`db.DB_PATH` 교체), 매 테스트 전 사용자 생성 테이블만 초기화(`ingredients`/`mode_warnings`는 시드 데이터라 유지)
  - `test_auth.py`(8개), `test_ingredients.py`(2개), `test_mode_warnings.py`(2개), `test_search.py`(2개), `test_scheduler.py`(3개) — 검색 캐싱은 `unittest.mock`으로 외부 API를 대체해 식품안전나라 상태와 무관하게 검증
  - `main.py`에 태그(검색/성분/인증/개인화/동기화) + `summary`/docstring 추가 → `/docs`(Swagger UI)가 카테고리별로 정리됨
  - 부수 정리: `ingredients.py`에 안 쓰이던 `DB_PATH` dead code 제거, `@app.on_event` deprecated 경고 발견 → `lifespan` 컨텍스트 매니저 방식으로 교체

### Phase 4 — 건강보조식품 원료 확보 (공공 API 확장) ✅ 핵심 완료 (잔여 이슈 남음)
**목표:** 식약처 API(C003)에 등록되지 않은(건강보조식품) 제품도 원료 분류를 보여줄 수 있도록, 추가 공공 API로 원료 데이터를 보강

**배경:** C003은 건강기능식품만 등록돼 있어서, "이너랩 위고잇 매드"처럼 건강보조식품으로 유통되는 제품은 검색은 되지만 원재료명(`RAWMTRL_NM`)을 받을 방법이 없음. 지금은 "건강기능식품으로 등록되지 않은 제품이에요" 안내만 뜨고 원료 분석은 못 함.

**방향 전환 (2026-07-12):** 원래 계획은 올리브영 크롤링이었으나 **폐기**. 실제 쇼핑몰 회원 약관 제17조 ①-3에서 "사전 승낙 없는 복제·목적 외 사용" 금지를 확인 — 크롤링 데이터를 우리 DB에 저장·노출하는 것 자체가 위반 소지. 대신 시중 유통 가공식품은 전부 식약처에 품목제조보고/수입신고가 돼 있다는 점을 활용해 공공 API로 대체 (합법 + 올리브영 취급 제품보다 커버리지 넓음):

| 제품 유형 | 데이터 소스 |
|---|---|
| 건강기능식품 | C003 (기존, 완료) |
| 국내 제조 건강보조식품 | **C002** 식품(첨가물)품목제조보고(원재료) — C003과 같은 식품안전나라 플랫폼 |
| 수입 건강보조식품 | **수입식품 수입신고별 한글표시사항** (`data.go.kr/data/15110213`) — 제품명(한/영)·수입업체·제조국·원재료명 포함 |
| 제품 이미지 + 알레르기·영양성분·바코드 | **HACCP 제품이미지 및 포장지표기정보** (`data.go.kr/data/15033307`) — Phase 5 제품 이미지 노출(차별화 포인트)까지 해결. 단, HACCP 인증 제품만 커버 |

- [x] 공공데이터포털(data.go.kr) 활용신청 — 계정 공용 인증키 1개 발급받아 `backend/.env`의 `DATA_GO_KR_API_KEY`로 저장. 식품안전나라와 별개 인프라라 **주간에도 정상 동작 확인됨**
- [x] C002 연동 — `search_service.py`를 `service_id` 파라미터로 일반화(캐시 키도 `C002:키워드`로 분리), `GET /api/search/general` 추가, mock 테스트 통과. 키에 C002 서비스 추가 신청 완료(2026-07-12, 기존 키 그대로 사용) — 정상 응답 구조 확인됨. ~~**실 데이터 검증만 대기**: 2026-07-12 밤에는 저녁 시간대(21시대·23시대 모두 확인)에도 C003·C002 모두 ERROR-500이라 검증 불가~~ → **[2026-07-13 완료] 실데이터 검증 성공.** 07-13 19시 정상화 후 실호출: C002 "비타민" 1,757건, `RAWMTRL_NM`(원재료명) 정상 반환 확인(C003도 4,708건 정상). 07-10~12 장애는 유지보수팀 문의로 원인·복구 확정(위 Phase 2 참고 노트)
- [x] HACCP API 연동 (`backend/data_go_kr.py`, `GET /api/search/haccp`) — 실 데이터 검증 완료 (유산균 16건, 원재료·알레르기·이미지 URL 확인). 주의: HACCP는 성공 코드가 `resultCode: "OK"`, 식약처는 `"00"` — 기관마다 달라서 둘 다 허용 처리
- [x] 수입식품 API 연동 (`GET /api/search/imported`) — 실 데이터 검증 완료 (비타민 5,728건). 검색 파라미터는 `prductKoreanNm`, 원재료는 `IRDNT_NM` (식품첨가물류는 null인 경우 있음)
- [x] 검색 흐름에 fallback 추가 (`src/api/foodSafetyApi.js` `searchSupplementFallback`) — C003 0건 또는 오류 시 C002 → HACCP → 수입식품 순 조회, 소스 하나가 죽어도 다음으로 넘어감. 각 소스 응답을 C003 필드 형태(`PRDLST_NM` 등)로 정규화해 기존 `ProductList`/`ProductDetail`/`parseIngredients` 그대로 재사용. fallback 결과에는 안내 배너 + 🟠 건강보조식품 뱃지(목록·상세), 상세에 식품유형·알레르기·영양성분·제조국 필드 추가. Playwright로 HACCP 경로(유산균)·수입식품 경로(시서스) 모두 e2e 검증 완료
  - 참고: HACCP `manufacture` 필드는 "업체명_주소" 결합 형태라 `_` 앞부분만 사용, 수입식품은 품목보고번호가 없어 리스트 key만 합성
- [ ] 제품명 매칭 로직 — 소스 간 제품명 정규화(공백·용량 표기 제거) + 유사도 매칭. 지금은 각 소스를 검색어로 직접 조회하는 구조라 시급성 낮아짐 — 나중에 "같은 제품을 여러 소스에서 병합"할 때 필요
- [ ] **판정 정확도 잠재 이슈 2건 (API 정상이어도 남는 문제, 서버 장애와 무관)** — 발견: 2026-07-13
  - ② **"C003 0건 = 건강보조식품" 가정의 허점**: C003 0건은 "미등록"이 아니라 "이 검색어로 등록명을 못 찾음"일 수 있음(예: "오메가3" 검색 → 등록명 "알티지 오메가쓰리" 미매칭). 진짜 건강기능식품인데 fallback으로 넘어가 🟠 건강보조식품으로 오분류됨. → 위 "제품명 매칭 로직"(검색어-등록명 정규화)과 연결됨
  - ③ **fallback 정밀도 문제** ✅ 해결(2026-07-20) — `src/utils/filterFallbackNoise.js` 신설, `foodSafetyApi.js`의 `searchGeneral`/`searchHaccp`/`searchImported`에 적용
    - HACCP(`prdkind`)·C002(`PRDLST_DCNM`): 식품유형에 비건강보조식품 키워드(가공유·과자·초콜릿·주류·김치·즉석식품 등)가 포함되면 제외 + 제품명에 반찬·간식 패턴(부각·샌드·초코볼·막걸리 등)이 있으면 "기타가공품"처럼 식품유형이 애매해도 제외. 실 API 데이터로 검증: "유산균" HACCP 16건→2건, "오메가3" HACCP 4건→0건(전부 오분류 식품이었음), "유산균" C002 60건→55건
    - 수입식품: `DCL_PRDUCT_SE_CD_NM`(신고구분) 필드가 "건강기능식품"인지로 직접 판별 — 식품유형 추측보다 정밀한 공식 근거. "유산균" 30건→29건, "초콜릿" 30건→1건
    - 트러블슈팅: 첫 구현 때 식품유형 값의 접미사 불일치(`"과자류"` 키워드가 실제 값 `"과자"`에 안 걸림, `"초콜릿가공품"`이 `"준초콜릿"`에 안 걸림)로 일부 누락 → 짧은 어근 키워드로 교체. 두 번째로 값에 공백이 섞인 경우(`"유산균 음료"`)를 놓쳐 실제 검색("요하이")에서 음료 1건이 새어나간 것을 Playwright 실사용 흐름에서 발견 → 비교 전 공백 제거로 해결(알레르기·성분분류 매칭과 같은 패턴)
    - 남은 한계: "기타가공품"처럼 식품유형 자체가 무의미한 카테고리는 이름 패턴에 없는 항목이면 여전히 통과함(예: 실제 보충제였던 "리포좀 오메가3 혼합"·"오메가369건강유" 등은 의도적으로 유지) — 100% 정밀도가 아니라 "명백한 오분류" 위주로 개선
- [x] **fallback 소스 우선순위 버그 수정 (2026-07-27)** — `searchSupplementFallback`이 `[searchGeneral(C002), searchHaccp, searchImported]` 순으로 조회해 "처음 매칭되는 소스"에서 멈추는 구조였는데, 이미지 필드는 HACCP만 갖고 있고 C002는 커버리지가 훨씬 넓어 거의 항상 먼저 매칭돼버려 HACCP(이미지 있음)까지 도달을 못 하는 버그였음. 순서를 `[searchHaccp, searchGeneral, searchImported]`로 변경. "노니"(C003 0건, C002 10건/이미지 없음, HACCP 1건/이미지 있음이 실제로 공존하는 키워드)로 Playwright 실기기 검증 완료 — 목록·상세 모두 정상 노출
- [ ] **C003 등록 제품(대부분의 정상 검색 결과)은 이미지 소스 자체가 없음 (2026-07-27 확인)** — "홀드잇" 사용자 리포트로 발견: C003(건강기능식품 정식 등록) API 응답엔 이미지 필드가 아예 없고, 해당 제품은 HACCP에도 미등록이라 지금 연동된 4개 공공 API 어디서도 이미지를 못 가져옴. C005 바코드연계제품정보(`svc_no=C005`)를 이미지 대안으로 조사했으나 필드 11개(`PRDLST_REPORT_NO`/`PRMS_DT`/`END_DT`/`PRDLST_NM`/`POG_DAYCNT`/`PRDLST_DCNM`/`BSSH_NM`/`INDUTY_NM`/`SITE_ADDR`/`CLSBIZ_DT`/`BAR_CD`) 중 이미지 URL 없음 — 공공데이터 경로는 막다른 길로 결론.
  - **[2026-07-30 막다른 길 확인] 네이버 쇼핑 검색 API도 사용 불가.** `developers.naver.com`의 검색 오픈API가 **NAVER API HUB**(네이버클라우드플랫폼 콘솔 기반, 인증 방식도 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY`로 변경)로 이관 중인데, 이관 대상은 블로그·뉴스·이미지·웹문서·백과사전·지식iN·카페글·지역 등 "콘텐츠 검색"뿐이고 **개별 상품명+이미지+가격을 반환하던 "쇼핑 검색"(`shop.json`)은 이관 없이 서비스 자체가 종료**됨. API HUB에 남은 "쇼핑인사이트"는 이름이 비슷하지만 트렌드/통계 API라 완전히 다른 기능 — 실제로 API HUB 신청 화면에서 "쇼핑"으로 검색해도 "쇼핑인사이트"만 뜨는 것으로 확인. `backend/naver.py`/`src/hooks/useNaverImage.js`(ProductThumb·ProductImage 연동까지 완료)는 구현은 해뒀지만 호출할 API가 없어져서 현재 미사용 상태로 보류 — 다른 이미지 소스로 교체 시 엔드포인트만 바꾸면 재사용 가능한 구조로 남겨둠.
  - **다음 이미지 소스 후보 (미조사)**: 쿠팡파트너스/11번가 오픈API(제휴 마케팅 목적 API라 무관한 앱에 이미지만 쓰는 게 약관상 허용되는지 확인 필요 — 올리브영 크롤링을 포기했던 사유와 비슷한 리스크), Google Custom Search 이미지 검색(무료 한도 하루 100건으로 낮음). 다음 세션에서 사용자와 우선순위 상의 후 진행
- [ ] 참고: C005 바코드연계제품정보 — 이미지 대안으로는 확인됨(위 항목), 원래 목적인 바코드 스캔 기능 확장 시엔 여전히 활용 후보

### Phase 5 — UI/UX 개선 ✅ 완료
**목표:** MZ 타겟 트렌디한 비주얼로 업그레이드 — 1차로 "클린 & 프레시"(흰 배경 + 민트그린 포인트, 2026-07-19)로 확정했다가, **"캡슐 팝"(민트→라임→옐로우 그라데이션 + 글래스모피즘, 2026-07-21, 클로드 디자인 MCP로 작업)** 으로 최종 전환

- [x] 제품 이미지 노출 (Phase 4의 HACCP API `제품이미지URL` 활용) — `ProductList` 썸네일(56px, 💊 플레이스홀더) + `ProductDetail` 대표 이미지(있을 때만), http→https 승격으로 배포 mixed-content 예방. 현재 이미지 제공 소스는 HACCP뿐이라 HACCP 경로(건강보조식품) 결과에 노출됨
- [x] 성분 등급 시각화 (그래프·뱃지·컬러 코딩) — `IngredientGradeBar.jsx` 가로 스택 바(경고→안전→미확인 순, 아이콘·라벨·개수 병기로 색맹 대비) + 등급 상수(`GRADE_ICON`/`GRADE_COLOR`/`GRADE_ORDER`)를 `ingredientGrade.js`로 일원화. 미확인 회색을 `#bfbfbf`→`#75808f`로 교체(흰 배경 대비 3:1 통과, CVD 검증 완료), 성분 상세 등급 뱃지·원재료 칩에도 아이콘 통일(미확인 ❓ 추가). Playwright mock 검증 완료
- [x] 성분 간 상호작용 분석 화면 — 상호작용 사전 8건 1차 큐레이션(시너지 4·주의 4, 출처 명시: NIH ODS·식약처·ISAPP 등)을 `backend/interactions.py` SQLite 시드로 구축, `GET /api/interactions`로 프론트가 앱 시작 시 1회 로드. 매칭은 로컬(`src/utils/findInteractions.js`, 키워드 포함 방식 — "푸마르산제일철"→철분). 상세 페이지에 🤝 좋은 조합 / ⏳ 시간차 섭취 권장 카드 패널(`IngredientInteractions.jsx`), 해당 제품에 양쪽 성분이 모두 있을 때만 표시. pytest 2건 + Playwright mock 검증 완료
- [x] 개인 맞춤 필터 (임산부·노인 모드) — Phase 3에서 저장 + 실제 하이라이트 연동까지 이미 완료됨. 비주얼 폴리시도 전역 디자인 토큰 적용에 포함해 완료(2026-07-19)
- [x] 반응형 디자인 완성 + 전체 비주얼 폴리시 (2026-07-19, 두 작업을 한 번에 진행) — 디자인 토큰(`index.css` CSS 변수: 브랜드 민트그린 `#0d9488`, 배경/서피스/텍스트/라운드/그림자)과 유틸 클래스(`.card`/`.segment`/`.btn-primary`/`.btn-ghost`/`.chip`/`.btn-plain`/`.banner-warn`) 도입, Pretendard 폰트(`index.html` CDN). `#root` 1126px 고정 → `max-width: 760px` 유동 컬럼, 검색창 300px 고정 → `flex:1`, 버튼 최소 높이 40px(터치 타깃). 브랜드색은 등급 '안전' 초록과 혼동 없게 청록 계열로 선택, 검증된 등급 상태색은 유지. 미사용 `App.css`(Vite 템플릿 잔재) 삭제. Playwright로 375/768/1280 뷰포트 목록·상세 캡처 + 가로 넘침 0px 확인
- [x] **"캡슐 팝" 테마로 재작업 (2026-07-21, 클로드 디자인 MCP 활용)** — 흰 배경 → 민트/라임/옐로우 그라데이션 배경(`--bg-gradient`, `fixed`)으로 전환, 글래스모피즘 도입(`.glass`/`.search-bar`/`.app-header` — 반투명+블러). 로고·뱃지 포인트 색상 `--accent-warm`/`--accent-yellow` 추가
  - `Header.jsx` 신규 분리 — 홈 화면은 로고+타이틀+로그인 칩, 그 외 화면은 ← 뒤로가기+제목으로 전환되는 상단 고정(sticky) 헤더
  - 홈 화면(`App.jsx`) 리디자인 — 캐치프레이즈 문구 추가, 검색/스캔 탭을 유리질 세그먼트로, 검색창을 알약형 글래스 바로, 하단에 "이런 걸 확인할 수 있어요" 안내 카드 3종 추가
  - `ProductList.jsx`에 `ProductThumb`(제품 썸네일, 이미지 없으면 그라데이션+💊 플레이스홀더), `ProductDetail.jsx`에 `ProductImage`(대표 이미지) 컴포넌트 정리
  - `AuthPanel.jsx` 로그인/회원가입을 유리질 세그먼트 탭 + 카드로 전환
  - `.env.local` 기반 목업 데이터 모드 추가(`src/api/mockData.js`, `VITE_MOCK_DATA=true`) — 식약처 API 낮 시간대(09~19시) 제한 중에도 실 API 없이 디자인 미리보기 가능하도록. **주의**: 이 값이 계속 켜진 채로 방치되면 실 API 정상 여부와 무관하게 목업 제품 2개 외엔 전부 "미등록"으로 뜨니, 실 데이터 검증 전엔 꼭 `false`인지 확인할 것
- [x] **개인화 설정 화면 리디자인 (2026-07-22, 클로드 디자인 목업 참고)** — `AuthPanel.jsx`의 로그인 후 화면을 계정 카드(아바타+이메일+로그아웃)/개인화 모드 카드(카드형 라디오)/알레르기 카드(있음·없음 세그먼트 + 19종 원형 아이콘 다중선택 그리드)로 재구성, 전부 동일한 `.card` 스타일로 통일. 클릭 즉시 저장하던 방식에서 **로컬 초안 상태 + "저장" 버튼**(dirty-check로 변경 없으면 비활성화) 방식으로 전환 — 저장 안 하고 새로고침하면 되돌아가고, 저장하면 유지되는 것까지 Playwright로 검증
- [x] **성분표 스캔 화면 리디자인 (2026-07-22, 클로드 디자인 목업 참고)** — `ImageScanUpload.jsx`를 기존 파란 버튼 방식에서 점선 드롭존(카메라 아이콘 + "사진 촬영/업로드" + "JPG, PNG 지원")으로 교체, `.upload-dropzone` 클래스 추가(호버 시 민트 강조)
- [x] **제품 상세 페이지 리디자인 (2026-07-23)** — 이미지가 없어도 항상 자리를 채우는 플레이스홀더(`ProductImage`, 사선 패턴 + 흰 알약 라벨) 도입. 성분 등급바(`IngredientGradeBar`)·분류 도넛차트(`IngredientCategoryBar`)를 같은 칩+바 문법으로 통일(도넛 폐기 — 브랜드 색과 무관하고 작은 구간에서 뭉툭해 보이는 문제 있었음, 화해 앱 참고). 알레르기·해외 논란 성분을 원재료 칩과 분리해 별도로 요약하는 `IngredientWarnings` 패널 신설. 상세 페이지 정보 구조 재편(기능성 강조박스, 섹션 구분선, 라벨/값 정보 그리드, 원재료 칩 8개 초과 시 "더보기"). ~~VITE_MOCK_DATA=true 상태로 작업해 실 API 최종 확인 전임 — 다음 세션에서 false로 되돌리고 검증 필요~~ → **[2026-07-27 완료]** 저녁 시간대 실 API로 검증. `VITE_MOCK_DATA`를 `false`로 되돌리고 실제 검색·상세 페이지 정상 동작 확인

- [x] **홈 랜딩페이지 도입 (2026-08-09~08-10, 클로드 디자인 산출물 이식)** — `CapsuleLayoutGreen.jsx` + `capsule-layout-green.css` 신규. 홈(`screen === 'home'`)을 풀블리드 랜딩으로 교체하고, `#root`의 `max-width:760px` 제약을 `.app-shell`로 옮겨 홈만 넓게 쓸 수 있게 분리. `capsule-layout-green.css`의 전역 `:root` 변수는 `.cg-root`로 스코프 축소(기존 화면 토큰 덮어쓰기 방지), `index.html`에 IBM Plex Mono 추가
  - 히어로 검색창을 실제 검색 로직에 연결(`App.jsx`의 `keyword`/`handleSearch`를 props로 전달), 타이핑 데모는 `placeholder`로 재활용. nav·하단 CTA는 `#cg-search` 앵커 + 스크롤/포커스로 연결
  - '실제 확인 화면 미리보기' 카드 동적화 — `src/utils/getPreviewProduct.js`(비비랩→락토핏→이너랩→닥터린 순 실검색), `src/utils/maskBrandName.js`(브랜드 가운데 마스킹). **마스킹은 홈 미리보기 전용** — 사용자가 직접 검색한 결과는 실명 그대로
- [x] **전 화면 톤앤매너 통일 — "캡슐 페이퍼" (2026-08-11)** — 브랜드 토큰을 티얼(`#0d9488`) → 세이지 그린(`#2f6f52`)으로, `--bg-gradient`를 크림 페이퍼(`#fbfaf5`→`#f1f0e9`)로 교체. 하드코딩된 티얼 RGB 그림자와 `ingredientCategory.js` 기능성원료 색도 함께 교체(등급색·상태색은 접근성 검증값이라 유지)
  - 리스트 카드 리디자인(썸네일 없을 때 건강기능식품=그린 캡슐/건강보조식품=앰버 캡슐, 기능성 문구 → 경고 성분 우선 해시태그 칩 3개), `.cg-container` 1040→760px로 폭 통일
  - 배경이 페이퍼로 바뀌며 무의미해진 글래스모피즘(`.app-header`/`.glass`/`--glass` 토큰) 제거 — **Phase 5 초기의 "캡슐 팝" 글래스 테마는 여기서 폐기됨**
  - **오래된 색상 모순 수정**: `IngredientDetail.jsx`·`IngredientConceptInfo.jsx`가 `CATEGORY_COLOR` 대신 하드코딩 색(`#1c5cab`/`#128a5e`)을 써서 같은 '기능성원료'가 화면마다 다른 색이던 문제 → import로 일원화
  - 랜딩 카피 중복 정리(같은 메시지 4회 반복 → '핵심 기능' 섹션 삭제 후 문제 카드에 병합) + **사실과 다른 문구 3건 수정**: "사진 한 장이면"(스캔 미구현), "만성질환자"(실제는 임산부·노인 2종 + 알레르기 19종), "인플루언서" 4회→2회
  - 회귀 버그 2건: ① 홈을 랜딩으로 교체하며 `Header`가 홈에서 렌더링 안 돼 **로그인/회원가입 진입점이 사라졌던 것** → 랜딩 nav에 로그인 버튼 추가 ② `.cg-root a { color:inherit }`가 `.cg-nav-cta`보다 명시도가 높아 CTA 글자가 검정으로 안 보이던 것 → `a.cg-nav-cta`로 수정
  - ⚠️ 이 작업 후 `.env.local`의 `VITE_MOCK_DATA`가 **`true`로 켜진 채 남아있음** — 실데이터 검증 전 반드시 `false`로 되돌릴 것
- [x] **검색 결과 화면 리디자인 + nav 전 화면 통일 (2026-08-12)** — 홈 랜딩의 nav를 `AppNav.jsx`로 분리해 홈·검색결과·상세·성분상세·로그인이 **같은 컴포넌트·같은 CSS를 공유**. nav가 `.app-shell`(760px) 밖으로 나가야 전체 폭을 쓸 수 있어 `App.jsx`를 `<AppNav /> + <div className="app-shell">` 구조로 변경. 변수는 `.cg-root`의 background/overflow까지 물려받으면 안 되므로 **변수 블록만 `.cg-scope`로 공유**
  - `Header.jsx`를 `← 검색 결과 · 2건` 라벨 줄로 재작성(랜딩 섹션 아이브로우와 같은 mono·자간), 기존 `.app-header` 폐기
  - **재검색 바** — 결과 화면에서 홈으로 돌아가야만 검색어를 바꿀 수 있던 문제 해결. 랜딩 히어로 검색창(`.cg-hero-search-box`)을 그대로 재사용하고 폭 제약만 덜어냄
  - `RotatingHeadline.jsx` — `오늘 먹은 '영양제'에 들어있는 성분은?`에서 따옴표 안 단어만 세로로 순환(전부 CSS 애니메이션, 리렌더 없음)
  - `ResultsView.jsx`/`ResultFilters.jsx` — 필터 4종(전체/인증 기능식품만/**낯선 원료 포함**/알레르기 제외) + 정렬 3종(정확도순/낯선 원료 많은 순/경고 성분 적은 순). '낯선 원료' 항목은 위 "성분 검색의 목적"에 맞춘 것. `useMemo`로 원재료 분석 1회만 수행
  - 텍스트 색 토큰을 랜딩 팔레트에 정렬(`--text-h` `#0c2b24`→`#17211b`, `--text-muted` `#6b8078`→`#4b564f`) — 8/11 브랜드 교체 때 이 토큰만 티얼 시절 값으로 남아 nav와 미묘하게 어긋나 있었음. **앱 전체 글자색에 영향**
  - nav 아래 구분선 추가, 결과 화면의 건강보조식품 안내 배너 제거(제품마다 붙는 뱃지로 구분) — 다만 "식약처 기능성 인증을 받지 않은"이라는 **설명이 앱에서 사라져** 노출 방식 재검토 필요
  - **[버그 수정] 검색 버튼이 hover해야 보이던 문제** — `.cg-root button{background:none}`(0,1,1)이 `.cg-hero-search-btn`(0,1,0)을 이겨 배경이 지워지고 흰 배경에 흰 글자가 됐음. `button.cg-hero-search-btn`으로 명시도 상향. **8/11 nav CTA 버그와 완전히 같은 패턴이라, 리셋 쪽을 `:where()`로 낮추는 정리가 필요**(미착수)
- [x] **제품 상세 리디자인 + 성분 상세 팝업 전환 (2026-08-18)** — 실제 화면 기준으로 문제 5가지(정보 중복·바 2개 혼동·빈 이미지 박스·구획 없음·업체명 위치)를 정리한 뒤 3안 비교 후 A안 채택. 카드 1개 → `제품/요약/경고/원재료/정보` 5개로 분리해 검색 결과·찜 목록과 같은 카드 문법으로 통일
  - `ProductSummary.jsx` 신설 — `전체 / 확인 필요 / 낯선 원료` 숫자 요약. '낯선 원료'를 한 칸으로 둔 건 상세 화면에서도 앱의 목적이 드러나게 하기 위함
  - **빈 이미지 박스 제거** — C003엔 이미지 필드가 없어 플레이스홀더가 항상 비어 있었음. 이미지가 실제로 있을 때만 렌더링
  - 분류 바(`IngredientCategoryBar`) 삭제 후 한 줄 텍스트로 — 등급 바와 축이 달라(안전도 vs 종류) 나란히 두면 혼동됨. 컴포넌트 파일도 미사용이 되어 삭제
  - `Modal.jsx` 신설 — 성분 상세를 별도 화면(`screen === 'ingredient'`)에서 **팝업**으로 전환. 화면이 통째로 바뀌어 스크롤 위치를 잃던 문제 해결. 배경 클릭·Esc·✕로 닫히고 열린 동안 본문 스크롤 잠금, 640px 이상에선 가운데 카드로 전환
  - 성분 팝업에 **판정 배너** 도입 — 칩만으로는 "그래서 먹어도 되나"가 안 읽혀서 결론을 문장으로 먼저 보여줌. 경고 사유(알레르기·논란)를 분류보다 먼저 확인하고, 미확인만 어두운 초록(랜딩 문제 섹션과 같은 재질)으로 `미확인 ≠ 미검증` 원칙을 구조로 표현
- [x] **성분 분류에 '식품 원료'(base) 신설 (2026-08-18)** — 우유가 '미확인'으로 뜨는 것을 사용자가 지적하면서 발견. **사전이 부족한 게 아니라 분류 체계에 칸이 없던 문제**였음(우유는 기능성원료도 첨가물도 아니고, 모르는 것도 아님)
  - `backend/ingredients.py`에 `BASE_INGREDIENTS` 110개 추가 — 사전 76개 → **190개**(기능성 52·첨가물 28·식품 원료 110). 곁가지로 레시틴을 첨가물에 추가(대두레시틴이 유화제인데 미확인으로 떨어지고 있었음)
  - CHECK 제약을 `ALTER TABLE`로 갱신하고, 시드를 `ON CONFLICT DO NOTHING`으로 바꿔 **사전을 늘리면 다음 실행에 자동 반영**되게 함(기존엔 테이블이 비었을 때만 시드해서 반영이 안 됐음)
  - ⚠️ **부분 문자열 매칭이라 짧은 사전 키가 위험** — `물`→녹차추출물, `밀`→밀크씨슬, `자당`→자당지방산에스테르. 최소 2글자 이상 + 다른 성분명의 부분이 되지 않는 이름만 넣음(`물`→`정제수`, `게`→`게살`). 판정 순서도 기능성원료·첨가물을 먼저 본 뒤 식품 원료를 확인해야 `대두`가 `대두레시틴`을 가로채지 않음
  - **부수 효과**: 그동안 '낯선 원료' 개수에 정제수·우유까지 세고 있었음 → 제품 상세 요약과 검색 결과 필터(`낯선 원료 포함`·`낯선 원료 많은 순`)가 실제 값이 됨
- [x] **분류 정확도 개선 — 실데이터 검증 (2026-08-18 19시 이후)** — 목업으로는 안 보이던 문제 3가지가 실제 원재료명에서 드러남
  - ⚠️ **오분류**: `카복시메틸셀룰로스칼슘`(증점제)이 이름의 `칼슘` 때문에 **기능성원료로 잡히고 있었음**. CLAUDE.md에 이미 적힌 `스테아린산마그네슘` 위험과 같은 유형인데 첨가물 사전에 없어 걸러지지 못한 것 → 첨가물 사전에 `셀룰로스` 추가(첨가물을 기능성보다 먼저 보는 순서를 활용)
  - **표기 흔들림**: 사전은 `결정셀룰로오스`인데 실제는 `결정셀룰로스`, 사전은 `코엔자임큐텐`인데 실제는 `코엔자임 Q10` → 패턴을 `/셀룰로오?스/`로, 사전에 `코엔자임` 추가
  - **패턴 누락**: `/색소$/`(끝소리)라 `식용색소적색제40호`를 놓침 → `/색소/`(포함)로 완화
  - **판정 순서 조정**: 첨가물 이름 패턴을 식품 원료 사전보다 **먼저** 확인하도록 변경. `키위향`(향료)이 식품 원료 `키위`에 먼저 걸려 식품으로 잘못 분류되던 문제
  - **사전 대폭 보강** — 인기 키워드 10개로 **제품 93개·성분 1,191개**를 분석해 미확인을 빈도순으로 정리한 뒤 명확한 것만 추가. 사전 190 → **249개**, 미확인 비율 **32% → 10%**
    - 기능성원료 +25(타우린·토코페롤·니코틴산·어유·헛개·울금·빌베리 등 — `토코페롤=비타민E`처럼 **같은 성분의 다른 이름**이 많았음)
    - 첨가물 +13(밀납·소르비톨·바닐린·빙초산·변성전분) / 식품 원료 +21(콩기름·해조·결명자·표고버섯·도라지·전분·초유)
  - ⚠️ **[2026-08-20 정정] 위 "미확인 32% → 10%"는 제품 93개 표본 기준이었음** — 전수 조사(아래) 결과 실제 미확인은 20.9%였음
  - **남은 120개는 의도적으로 미확인 유지** — `혼합제제`·`기타가공품`·`식물혼합추출물분말`처럼 이름만으로는 정말 알 수 없는 것들. 억지로 분류하면 자체 큐레이션의 오분류 위험이 커지므로 `미확인 ≠ 미검증` 원칙대로 둠
  - **회귀 검증 17개 케이스 통과** — 사전을 늘릴수록 이름 충돌이 늘어나므로, 겹치는 쌍(`밀가루`↔`밀크씨슬`, `대두`↔`대두레시틴`, `키위농축`↔`키위향`, `옥수수전분`↔`변성전분`, `블루베리`↔`빌베리`)을 일부러 골라 확인
- [x] **홈 히어로 2단 리디자인 (2026-08-20)** — 외부 피드백 "전형적인 클로드로 작업한 웹페이지 같다"에서 출발. 원인은 `히어로 → 미리보기 → 카드 3개 그리드 → CTA 배너` 순서가 **어느 SaaS 랜딩페이지에나 있는 뼈대**였던 것(캡슐 테마를 입혀놔도 구조가 그대로 드러남)
  - 방향 탐색: 스토리 스크롤 / 카드 캐러셀 / 앱 홈(피드) 3안 → "앱용 같다" 피드백 → 브라우저 폭 기준으로 재작업(에디토리얼 스크롤 / **매거진 그리드** / 포털 대시보드) → 매거진 그리드 채택
  - **세 섹션을 히어로 하나로 합침**(`cg-scan`·`cg-problems` 삭제) — 이 앱이 뭘 하는지가 첫 화면에서 끝나도록. 왼쪽은 카피+검색, 오른쪽은 확인 카드 + 문제 카드 캐러셀
  - **확인 카드는 순환에서 빼고 고정 노출** — 이 앱의 결과물을 보여주는 유일한 실물이라 25%만 보이는 건 손해
  - **확인 카드에 '낯선 원료' 줄 추가** — 유명 성분 비교가 아니라 낯선 원료를 알려주는 게 이 앱의 목적인데 히어로에 그 축이 없었음. `buildPreview()`에서 `category === 'unknown'`을 뽑아 **실제 파싱 결과**를 표시
  - `ProblemCarousel`: 5초 자동 전환 + **마우스/키보드 포커스 시 정지**, 화살표·점으로 수동 이동. 카드마다 문장 길이가 달라 뷰포트 높이를 현재 카드에 맞춰 따라가게 함(가장 긴 카드 기준이면 빈 여백이 생김). 질문은 말풍선 + 간단한 사람 일러스트로
  - `useTypingSearch(paused)`: 검색창 포커스 시 타이핑을 멈추고 placeholder를 비움 — 직접 입력하려는데 문구가 계속 바뀌면 방해가 되므로
  - **폭을 랜딩만 1040px로 되돌림** (2026-08-11에 760px로 좁혔던 것) — 2단 히어로를 760px에 넣으면 왼쪽 칼럼이 350px이 되어 헤드라인이 뭉개짐. `.cg-container`는 홈에서만 쓰이고 nav는 `.cg-root` 안일 때만 넓혀 **다른 화면(`.app-shell` 760px)에는 영향 없음**. 되돌리려면 CSS 두 줄만 지우면 됨
  - 히어로에 민트그린 띠 배경 추가 → 같은 색이던 탭 배경(`--tint-1`)과 거의 같던 신뢰 칩 배경(`--green-soft`)이 묻혀서 함께 조정. 떠다니는 캡슐도 확인 카드 위에 겹쳐서 제목 옆으로 옮기고, 밝은 쪽이 배경과 같은 계열이라 `--capsule-ivory` 신설
  - **[버그] 캐러셀 점·화살표가 세로로 늘어난 타원이 됨** — flex `stretch`인 줄 알고 `align-items:center`를 넣었지만 안 고쳐졌고, 실제 원인은 `index.css`의 전역 `button{min-height:40px}`(터치 타깃용)이 `height:6px`를 이긴 것. `min-height:0` 명시로 해결. 목업 HTML엔 `index.css`가 없어 안 드러났던 차이. **nav CTA·검색 버튼 버그와 같은 계열이라 리셋을 `:where()`로 낮추는 정리가 여전히 필요**(미착수)
- [x] **홈 하단 아티클 — 앱 데이터로 직접 작성 (2026-08-21)** — `src/data/articles.js` 신설, 5편
  - **외부 기사를 옮기지 않은 이유는 저작권** — 전문은 물론 요약도 2차적저작물 작성권에 걸릴 수 있어, 저작권 대상이 아닌 **사실만** 정리(올리브영 크롤링을 포기했던 것과 같은 판단)
  - 소재가 전부 앱 안에 있었음 — 성분 사전 395개, 알레르기 19종, 논란 첨가물 6종, 사전 작업에서 실제로 만난 사례(카복시메틸셀룰로스칼슘 오분류, 2020년 유산균 속 재분류)
  - **팝업은 기존 `Modal.jsx` 재사용** — 새 라우트 불필요, 앱 안에서 읽히는 경험이 일관됨
  - CMS가 없으므로 이 배열이 곧 원본. 5개 규모라 별도 저장소를 두지 않음
  - ⚠️ 아티클 카드를 `<button>`으로 바꾸며 전역 `button` 규칙(padding/min-height/border-radius)을 다시 눌러줘야 했음 — 캐러셀 점·화살표와 같은 함정
  - 남은 것: 이모지 썸네일은 여전히 임시(이미지 소스가 없는 프로젝트 제약상 타이포그래피 커버가 후보)
- [x] **알레르기 개수 오류 수정 (2026-08-21): 21종 → 19종** — `allergens.js` 배열은 19개인데 주석과 **사용자에게 보이는 문구**(`IngredientDetail.jsx` 2곳)가 "표시 대상 21종"이라고 안내하고 있었음. 식약처 공식 표시 대상도 19종이므로 데이터가 맞고 문구가 틀린 것. 주석의 "참깨·들깨 추가(24종)"도 틀린 계산(19+2=21)이라 함께 정정. `ResultsView.jsx`·`index.css`·README·CLAUDE.md까지 전부 맞춤
  - 교훈: **문서·화면 문구에 적힌 개수는 데이터와 어긋날 수 있음.** 아티클을 쓰며 데이터와 대조하다 발견했고, `allergens.js`에 "배열 항목 수와 어긋나지 않게 확인" 메모를 남김
- [ ] **nav '성분 사전' 화면 — 만들지 미결 (2026-08-20)** — 리디자인 목업에 넣었던 nav 링크 3개(`제품 검색`/`성분 사전`/`내 영양제`)는 실제로 존재하지 않는 화면이었음(실제 `AppNav`는 로고 + 로그인 드롭다운뿐). 다만 **`GET /api/ingredients/categories`가 사전 249개 전체를 이미 내려주고 있어** 백엔드 작업 없이 프론트 목록 화면만 만들면 됨. 위 아티클 방향(사전 재활용)과 이어짐
- [x] **성분 분류 — C003 전수 조사 기반 보강 (2026-08-20): 미확인 20.9% → 15.2%**
  - **핵심 발견: 검색어 없이 조회하면 C003 전체가 나옴** (44,691건, 한 번에 최대 1,000건 = 46페이지). 그동안 키워드로 표본만 뽑던 것과 달리 전수 조사가 가능해짐
  - **접근 방식 — 수집과 판단을 분리**: API가 09~19시에 막히므로, 시간 제약이 있는 "수집"만 밤에 끝내고 아무 때나 가능한 "분류 판단"은 저장된 파일로 진행. 한 번 받아두면 API 시간대와 무관하게 사전 작업 가능
  - **개별 이름 추가보다 패턴이 효율적**이었음 — 미확인 3,935종을 무리로 묶어 규모를 재보니:
    - 유산균 학명 127종·17,938회(13.3%) / L-아미노산 85종·7,781회(5.8%) / 가공품류 71종·21,389회(15.9%) / 올리고당·당알코올 47종·5,783회(4.3%) / **추출물류 1,786종·46,794회(34.7%)**
  - 적용: `FUNCTIONAL_PATTERNS` 신설(유산균 학명 접두 + `/^L-/`), 가공품·올리고당·당알코올을 사전에 추가. **사전 249 → 277개**
    - 유산균은 2020년 Lactobacillus 속 재분류로 표기가 다양(`Lactiplantibacillus`·`Lacticaseibacillus`·`Limosilactobacillus`…) → 접두 패턴 두 줄로 커버
    - `D-`/`DL-`은 기능성 패턴에서 제외 — `D-말티톨`처럼 당알코올 감미료가 섞임
    - `올리고당` 한 항목이 부분 문자열 매칭으로 갈락토/자일로/이소말토/말토올리고당까지 전부 커버
    - 당알코올(말티톨·글리시톨)을 첨가물에 넣어, 첨가물 사전이 먼저 검사되는 순서 덕에 `말티톨시럽`=첨가물 / `메이플시럽`=식품으로 갈림
  - **`기타가공품`(6,269회)은 의도적으로 미확인 유지** — 식품 종류를 전혀 알 수 없는 표기라, 분류되는 `곡류가공품`·`농산가공품` 등과 구분
  - 곁가지 교정: `Lactiplantibacillus plantarum C29 프로바이오틱스와 발효대두분말의 복합물`이 이름 속 `대두` 때문에 식품 원료로 잡히던 것 → 기능성원료로 바로잡힘
  - **도구 3종 신설** (분석·검증은 API 불필요):
    - `backend/harvest_c003.py` — 전체 원본 수집 → `backend/data/c003_raw.json`(37.8MB, gitignore)
    - `backend/analyze_unknown.mjs` + `run_analyze.sh` — 미확인 빈도 순위
    - `backend/check_categorize.mjs` + `run_check.sh` — 회귀 확인 34케이스
    - ⚠️ 분석·검증 스크립트는 **앱의 JS 판정 로직을 그대로 import**함(파이썬 재구현 금지 — 사전을 고칠 때마다 분석 결과와 화면 판정이 어긋남). 소스 import에 확장자가 없어 Node가 직접 못 읽으므로 **esbuild로 번들해 실행**
- [x] **성분 분류 — 상위 미확인 중 확실한 것 등록 (2026-08-20): 미확인 15.2% → 10.9%** (당일 누적 20.9% → 10.9%)
  - 사전 277 → **323개**. 기능성원료(엠에스엠·이노시톨·옥타코사놀·포스파티딜세린·키토산·이눌린·베타카로틴·홍국·은행잎·흰강낭콩 등), 첨가물(지방산에스테르·글루콘산·인산염류·감미료·카민·비트레드), 식품 원료(대나무수액·마카·다시마·쌀겨·목화씨·요구르트 등)
  - `피리독신` 한 항목으로 `피리독신염산염`까지, `지방산에스테르` 한 항목으로 `소르비탄~`·`자당~`까지 커버 — **BASE_INGREDIENTS 주석이 경고하던 `자당 → 자당지방산에스테르` 오분류도 여기서 해소**(첨가물 사전이 식품 원료보다 먼저 검사되는 순서 덕)
  - `ADDITIVE_PATTERNS`에 `/향분말$/` 추가 — `/향$/`만으로는 `요구르트향분말`을 놓쳤음. `향`을 통째로 넣지 않은 건 향신료가 섞이기 때문
  - ⚠️ **회귀 케이스를 늘리다 기존 버그 발견**: `정향`(향신료=식품)이 `/향$/`에 걸려 첨가물로 분류되고 있었음 → 향료 패턴보다 먼저 빼내는 예외(`정향`·`회향`·`팔각향`) 추가. 전수에서 2회뿐이라 영향은 작지만 걸리면 명백한 오분류
  - 회귀 케이스 34 → **46개**, 전부 통과
- [x] **성분 분류 — 추출물류 정리 (2026-08-21): 미확인 10.9% → 6.3%** (사전 323 → 395개, 회귀 케이스 46 → 63개)
  - **접미사를 떼고 어근으로 합산**한 게 핵심 — `참당귀추출분말`·`참당귀농축액`처럼 같은 원료가 표기만 달리 흩어져 있어서, `~추출물/농축액/분말`을 떼니 935개 어근·36,756회로 정리됨. 어근만 넣으면 부분 문자열 매칭으로 모든 표기가 걸림(`병풀` 하나로 병풀잎추출물·병풀추출분말까지)
  - **분류 기준**: 식약처 고시·인정 기능성 원료이거나 기존 사전과 같은 계열이면 기능성원료(`강황`=울금 / `흑삼`=인삼·홍삼 / `미르틸루스산앵도`=빌베리 / `콜레우스포스콜리`), 그 외 알아볼 수 있는 식물·한약재·과일은 **식품 원료**(참당귀·산수유·여주·구기자·감초·황기 등 37종)
    - 식물을 식품 원료로 두는 이유: `낯선 원료`는 **모르는 원료**를 뜻하는데 산수유·감초는 낯설지 않음. 억지로 기능성으로 올리면 자체 큐레이션 오분류가 커짐
  - **`식물혼합농축액`(1,186회)은 미확인 유지** — 무엇이 섞였는지 안 적혀 있음. 반대로 `야채혼합`·`비타민혼합`처럼 종류를 알 수 있는 것은 분류함
  - 2차 배치: 기능성(NAG·글리신·베타인·락토페린·글루코만난·식물스테롤·뮤코다당·로얄젤리) / 첨가물(식품첨가물·캡슐류·풀루란·멘톨·주정·로커스트콩검·패각·호박산) / 식품(알파미분·보라지·중쇄중성지방·유크림·유채·퉁퉁마디·한천). 학명 패턴에 `clostridium` 추가
  - **순서로 푼 충돌**: `호박산`(산도조절제)을 첨가물에 넣어 식품 `호박`이 `호박산나트륨`을 가로채지 않게 함. `l-멘톨`은 소문자라 `/^L-/`에 안 걸리지만 `멘톨`(향료)로 올바르게 잡힘
  - ⚠️ **발견: 사전 시드가 INSERT만 하고 삭제는 안 함** — `로얄젤리`를 기능성으로 올리며 `로얄젤리가공품`을 코드에서 뺐는데 DB에는 남아 있었음(이번엔 기능성 사전이 먼저 검사돼 무해). **항목을 제거할 때는 DB 행도 함께 확인할 것**
- [ ] **남은 미확인 6.3%(40,915회) — 대부분 손댈 수 없는 것** — `식물혼합농축액`(무슨 식물인지 없음)·`000`·`000개/g 이상`(데이터 오류)·`6년근`(조각난 표기)·효소류. 전체 순위는 `backend/data/unknown_ranked.json` — 미확인 중 제일 큰 덩어리지만 **일괄 분류하면 안 됨**(식물마다 기능성/식품이 갈림). 상위 빈도 것만 개별 등록하는 게 현실적. `backend/data/unknown_ranked.json`에 전체 순위가 있음
- [x] **목업 데이터에 실제 API 응답 저장 (2026-08-18)** — 기존 목업 2개는 손으로 만든 것이라 실제 응답과 필드 구성이 달랐음. `홀드잇` 검색 결과 2건을 **17개 필드 그대로** `src/api/mockData.js`에 추가(총 4개). 식약처 API가 막히는 **09~19시에도 실데이터와 같은 화면**으로 UI를 확인할 수 있게 됨
- [x] **검색 결과 페이지네이션 — 더보기 (2026-08-20)** — `end_idx=10` 고정에 더보기가 없어, `총 4,708건`이라고 안내하고 10개만 보여주는 막다른 길이었음. 위 필터·정렬도 **불러온 10건 안에서만** 동작해 표본을 늘리는 게 선행돼야 했음
  - `App.jsx`: 검색 성공 시 `results`에 **검색어를 함께 저장** → 더보기를 누르는 사이 입력창을 고쳐도 원래 검색어로 이어서 조회. `handleLoadMore()`가 다음 10개를 받아 기존 목록에 이어붙이고, 실패해도 기존 목록은 유지하고 에러만 표시
  - `ResultsView.jsx`: `hasMore`일 때만 하단에 `더보기` 버튼 노출
  - **건강보조식품 fallback 경로는 범위 밖** — HACCP/C002/수입식품 세 소스를 순회하는 구조라 페이지 개념을 넣기엔 복잡도가 커서 별도 과제로 남김(`fallbackUsed`면 버튼 자체가 안 뜸)
  - 실데이터 검증: 락토핏 `start_idx=1~10`과 `11~20`이 서로 다른 제품을 반환하고 `totalCount`(57)는 유지되는 것 확인. **브라우저 실사용 확인은 미완**
  - 남은 한계: 필터·정렬은 여전히 **불러온 만큼만** 적용됨(전체 4,708건 기준이 아님). 원재료를 파싱해야 판정이 되는데 식약처 API엔 그런 정렬 기능이 없어서, 표본이 커진 것이지 근본 해결은 아님
- [x] **검색 대기 로딩 화면 (2026-08-20, `LoadingCapsules.jsx`)** — 식약처 응답이 5~18초까지 걸리는데 그 사이 화면이 `검색 중...` 한 줄뿐이라 멈춘 것처럼 느껴졌음. 돋보기 주위를 캡슐이 도는 궤도 애니메이션으로 교체
  - 궤도 캡슐 3개 색을 **실제 성분 분류 색**(`CATEGORY_COLOR`: 기능성원료/첨가물/식품 원료)과 맞춤 — 장식이 아니라 "지금 원재료를 분류하는 중"이라는 뜻
  - 궤도 크기를 px 고정 대신 `%` + `aspect-ratio`로 잡아 반응형 대응, pulse 그림자는 하드코딩 rgba 대신 `color-mix(var(--brand))`
  - `role="status"`로 감싸고 순환 문구·애니메이션은 `aria-hidden` (스크린리더가 1.7초마다 바뀌는 문구를 반복해 읽지 않게), `prefers-reduced-motion` 대응

### Phase 6 — OCR 스캔 기능 (후순위로 조정)
**목표:** 성분표 사진 → 자동 분석

**후순위 사유:** Claude API 비전 기능은 Claude.ai 구독이 아니라 **종량제(사용한 만큼 과금)** 방식 — 사용자가 스캔할 때마다 비용 발생. 이 과금 구조가 원하는 방향이 아니라 판단해 뒤로 미룸. Phase 4(공공 API 확장)가 같은 문제(건강보조식품 원료 확보)를 과금 없이 풀 수 있는 대안이라 우선순위를 밀어둠. 진행하게 되면 이미지는 반드시 Phase 2에서 만든 백엔드를 거쳐 Claude API를 호출해야 함 (API 키 노출 방지).

- [x] 카메라/이미지 업로드 UI (`ImageScanUpload.jsx` — 미리보기까지 완료, 분석 연동은 보류)
- [ ] 백엔드에 Claude API 이미지 입력 엔드포인트 추가
- [ ] 추출된 성분 자동 매핑 및 등급 판정
- [ ] 스캔 정확도 테스트 및 보정

### Phase 7 — 배포 ⬅ 진행 중 (Railway/Vercel 배포·CORS 연결까지 완료, 스모크 테스트부터 남음)
**[2026-07-22 결정] 호스팅 방향**: 프론트 Vercel/Netlify는 그대로, **백엔드는 Railway + Railway 관리형 Postgres**로 확정(SQLite에서 마이그레이션). 사용자가 "포트폴리오 어필"을 최우선 기준으로 요청해서 나온 결론 — Railway가 Postgres·백그라운드 워커(스케줄러)를 대시보드에서 바로 지원하고, "SQLite 파일 하나" 대비 "관리형 DB로 마이그레이션"하는 스토리가 어필에 더 유리하다고 판단.

- [x] **SQLite → Postgres 마이그레이션 (2026-07-31 완료)** — `db.py`/`auth.py`/`ingredients.py`/`interactions.py`/`mode_warnings.py`/`scheduler.py` 전부 전환(`?`→`%s`, `conn.execute()`→커서 방식, `AUTOINCREMENT`→`SERIAL`, `lastrowid`→`RETURNING id`, `auth.py`의 `sqlite3.OperationalError` 하위호환 땜빵 삭제). `db.py`의 `get_connection()`을 `@contextmanager`로 재작성(성공 시 commit/예외 시 rollback/항상 close). 로컬 개발도 SQLite 대신 Railway Postgres의 `innerbeauty_dev` DB를 그대로 사용(운영과 이원화하지 않음, 드리프트 방지), 테스트는 `innerbeauty_test`로 분리(`conftest.py`가 `TEST_DATABASE_URL`로 교체 + `TRUNCATE ... RESTART IDENTITY CASCADE`로 SERIAL 시퀀스까지 리셋). pytest 31개 전체 통과 + 회원가입·로그인·개인화 설정 저장(ON CONFLICT upsert)·스케줄러까지 실서버로 검증 완료. 참고: 매 쿼리마다 새 커넥션을 여는 방식(풀링 없음)이라 네트워크 왕복 때문에 SQLite 대비 확연히 느려짐(pytest 31개에 347초) — 포트폴리오 스케일에선 감수하는 의도된 단순화
- [x] `backend/Procfile`(`web: uvicorn main:app --host 0.0.0.0 --port $PORT`) 작성, `main.py` CORS에 `allow_origins=[FRONTEND_URL]` 추가(기존 로컬 `allow_origin_regex`와 병행)
- [x] **GitHub push + Railway/Vercel 배포 + CORS 연결 (2026-08-05 완료)** — 저장소 `github.com/withwizz1-spec/innerbeauty-checker`(main). 백엔드: Railway(Root Directory `backend/`, 환경변수 7개 등록) → `https://innerbeauty-checker-production.up.railway.app`, `/docs` 200 및 실검색 API curl 검증 완료. 프론트: Vercel → `https://innerbeauty-checker.vercel.app`. CORS는 Railway `FRONTEND_URL`을 Vercel URL로 갱신 후 재배포, `curl -X OPTIONS`로 `access-control-allow-origin` 헤더 일치 확인
  - 트러블슈팅: Vercel의 모노레포 자동 감지가 저장소 안 `backend/`(FastAPI)까지 별도 서비스로 잡아 화면에 계속 나타났음 — 실제 배포 설정(Root Directory `./`, Build/Deploy)은 단일 프로젝트 기준으로만 존재해 그대로 진행하면 프론트만 배포되는 구조라 무시하고 진행
- [ ] 최종 스모크 테스트 (Railway `/docs`, Vercel URL에서 실사용 흐름 전체, CORS 에러 없는지, 스케줄러 1시간 잡 정상 실행) — 브라우저 실사용 확인만 남음
  - ~~⚠️ 2026-08-09 이후 작업분이 전부 미커밋 상태라 배포본에는 미반영~~ → **[2026-08-20 해소]** 커밋 3개로 분리해 main 병합·push 완료(`8dc4b05`·`5ae7423`·`f881317`). Railway/Vercel 자동 배포가 걸려 있어 8/9 이후 작업분이 처음으로 배포본에 반영됨. **다만 브라우저 실사용 확인 전에 배포된 상태**라 스모크 테스트에서 함께 확인 필요
- [ ] 성능 최적화 및 에러 핸들링
- [ ] 포트폴리오 정리

---

## 작업 시 참고
- 새 기능 작업 전, 위 로드맵에서 "현재 단계"가 어디인지 먼저 확인할 것
- 막히는 이슈는 간단히 원인과 해결 방향만 정리 (장황한 설명 지양)
- 세션 종료 전: 오늘 작업한 내용을 사용자가 노션 개발 일지에 직접 정리할 수 있도록 요약 제공
- **노션 등에 긴 한글 텍스트를 작성/수정한 뒤에는, 제출 전 반드시 다시 읽어서 오타(특히 받침이 바뀌는 오류: 도넛→도넫, 마그네슘→마그네슈, 팔레트→팜레트 등)가 없는지 검토할 것.** 한 번에 길게 쓰기보다, 쓰고 나서 검토하는 습관이 중요함
- **노션 개발일지를 업데이트할 때는, 그 세션에서 완료/변경된 작업을 CLAUDE.md 로드맵 체크리스트에도 함께 반영할 것.** 두 문서가 따로 놀지 않도록, 노션 업데이트와 CLAUDE.md 업데이트를 한 세트로 처리 (완료 항목 체크, "현재 단계" 화살표 이동, 새로 발견된 이슈/보류 사항 기록 등)

## 개발 일지 작성 (연동된 노션에 업데이트)
- 매 세션 종료 전, 또는 "오늘 한 일 노션에 업데이트 해줘" 요청 시, 오늘 개발한 내용을 아래 형식으로 정리한 뒤 노션 개발 일지에 업데이트할지 물어본다.

### 속성(properties) 입력
| 속성 | 내용 |
|---|---|
| 제목 | 오늘 작업 핵심 한 줄 요약 (예: 식약처 API 연동 완료) |
| 날짜 | 오늘 날짜 |
| 프로젝트 | 작업한 프로젝트명 |
| 오늘 한 일 | 완료한 작업 목록 (짧게) |
| 된 것 | 의도대로 잘 된 것 |
| 안 된 것 | 해결 못 했거나 막힌 것 |
| 다음에 할 것 | 다음 세션에서 이어서 할 작업 |
| 작업 시간 | 예: 2시간 |
| 컨디션 | 😊 / 😐 / 😴 중 선택 |

### 속성 값 내부 서식 (항목이 여러 개일 때)
한 속성 안에 여러 항목을 나열할 때 `/`로 구분하지 말고, **줄바꿈으로 항목을 나누고 각 줄 앞에 이모지를 붙일 것.**
- `오늘 한 일`: 각 줄 앞에 🔘
- `된 것`: 각 줄 앞에 ✅
- `안 된 것`: 각 줄 앞에 ❗️
- 그 외 여러 항목이 들어가는 속성(예: `다음에 할 것`)도 `/` 대신 줄바꿈으로 구분

### 본문(content) 작성 규칙 — 가독성 중요
속성만 채우지 말고, 반드시 본문도 아래 형식으로 상세하게 작성할 것. **가독성이 핵심.**

1. 맨 위에 `## 📋 작업 상세 내용` 헤딩
2. 작업 항목마다 `### 번호. 제목 ✅` 형식의 소제목
3. 소제목 사이에 구분선(`---`) 넣기
4. 각 항목은 불릿으로 구체적으로 작성 — **무엇을, 왜, 어떻게** 했는지
5. 여러 선택지를 비교한 내용은 표(table)로 정리
6. 트러블슈팅이 있으면 `### 🔧 트러블슈팅` 섹션 추가
7. 새로 배운 개념이 있으면 `### 📚 배운 개념` 섹션 추가
8. 코드·명령어는 인라인 코드(백틱)로 표시

### 톤
- 간결하지만 나중에 다시 봐도 이해되도록 맥락 포함
- 단순 나열이 아니라 "왜 그렇게 했는지" 이유를 남길 것

---
# 문제 해결 일지 작성 규칙 (🐛 문제 해결 모음 DB)

## 원칙
- **속성(property) = 짧은 메타데이터만.** DB 뷰에서 훑어볼 용도. 절대 통짜 텍스트 넣지 않기.
- **페이지 본문(body) = 실제 서술.** 헤더/코드블럭/목록으로 구조화.
- 하나의 시도(try)는 하나의 목록 항목. 절대 한 문단에 몰아쓰지 않기.
- 에러 메시지, 코드, SQL, 콘솔 로그는 예외 없이 코드블럭(```).

## 속성 값 작성 규칙 (짧게, 1줄)
| 속성 | 규칙 |
|---|---|
| 문제 요약 (title) | 에러 메시지 or 핵심 증상 한 줄. `[프로젝트] 에러명` 형식 권장 |
| 문제 상황 | 1~2문장 요약만. 상세 재현 조건은 본문 "문제 상황" 섹션으로 |
| 시도한 것들 | **사용하지 않음** (본문으로 이관) — 또는 "N가지 시도, 본문 참고" 정도만 |
| 최종 해결 | 한 줄 결론(원인 + 해결책 키워드)만. 상세 코드/설명은 본문으로 |
| 난이도 / 해결 여부 / 프로젝트 / 날짜 / 참고 링크 | 기존 그대로 (짧은 값이라 문제 없음) |

## 페이지 본문 템플릿 (고정 순서)

```markdown
## 🐞 문제 상황
- 어떤 동작을 했을 때 발생했는지 (버튼 클릭, API 호출 등)
- 에러 메시지:
\`\`\`
(콘솔/로그 원문)
\`\`\`
- 관련 코드 스니펫 (있다면)

## 🔍 원인 분석 (선택)
- 처음에 의심한 원인
- 왜 그 의심이 틀렸는지 / 맞았는지

## 🔧 시도한 것들
1. **[시도 요약]** → 결과: (통과/실패/부분 해결)
2. **[시도 요약]** → 결과: ...
3. ...

## ✅ 최종 해결
**원인**: (한 줄)

**해결 코드**:
\`\`\`sql
-- 또는 js, ts 등
\`\`\`

## 💡 교훈
- 다음에 비슷한 상황에서 뭘 먼저 의심할지, 규칙화된 한 줄
```

## 이모지 규칙
- 🐞 문제 상황 / 🔍 원인 분석 / 🔧 시도한 것들 / ✅ 최종 해결 / 💡 교훈
- 섹션당 이모지 1개 고정, 남발하지 않기

## Claude Code에게
- 문제 해결 로그를 작성할 때 **반드시 위 템플릿 순서**로 본문(content)을 채울 것
- 속성 필드에는 절대 여러 문장/코드/줄바꿈 몰아넣지 말 것
- "시도한 것들"이 1개뿐이면 "🔧 시도한 것들" 섹션은 생략하고 바로 "✅ 최종 해결"로
- 원인이 처음부터 명확했다면 "🔍 원인 분석" 섹션은 생략 가능
